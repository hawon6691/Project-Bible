name: shop-java-springboot-maven-mysql CI

on:
  pull_request:
    paths:
      - ".github/workflows/shop-java-springboot-maven-mysql-ci.yml"
      - "Backend/shop/shop-java-springboot-maven-mysql/**"
      - "Database/mysql/shop/**"
      - "Tools/ci/load_mysql.sh"
  push:
    branches: [main]
    paths:
      - ".github/workflows/shop-java-springboot-maven-mysql-ci.yml"
      - "Backend/shop/shop-java-springboot-maven-mysql/**"
      - "Database/mysql/shop/**"
      - "Tools/ci/load_mysql.sh"
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    working-directory: Backend/shop/shop-java-springboot-maven-mysql

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: project_bible
          MYSQL_DATABASE: mysql
          MYSQL_USER: project_bible
          MYSQL_PASSWORD: project_bible
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -pproject_bible"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20
    env:
      MYSQL_HOST: 127.0.0.1
      MYSQL_PORT: 3306
      MYSQL_ROOT_PASSWORD: project_bible
      MYSQL_USER: project_bible
      MYSQL_PASSWORD: project_bible
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      DB_NAME: pb_shop
      DB_USER: project_bible
      DB_PASSWORD: project_bible
      JWT_SECRET: ci-access-secret
      JWT_REFRESH_SECRET: ci-refresh-secret
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y default-mysql-client
        working-directory: .
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - run: chmod +x Tools/ci/load_mysql.sh
        working-directory: .
      - run: bash Tools/ci/load_mysql.sh shop pb_shop
        working-directory: .
      - run: chmod +x mvnw
      - run: ./mvnw -B test
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: shop-java-springboot-maven-mysql-test-reports
          path: Backend/shop/shop-java-springboot-maven-mysql/target/surefire-reports/
          if-no-files-found: ignore
