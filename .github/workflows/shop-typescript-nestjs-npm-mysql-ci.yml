name: shop-typescript-nestjs-npm-mysql CI

on:
  pull_request:
    paths:
      - ".github/workflows/shop-typescript-nestjs-npm-mysql-ci.yml"
      - "Backend/shop/shop-typescript-nestjs-npm-mysql/**"
      - "Database/mysql/shop/**"
      - "Tools/ci/load_mysql.sh"
  push:
    branches: [main]
    paths:
      - ".github/workflows/shop-typescript-nestjs-npm-mysql-ci.yml"
      - "Backend/shop/shop-typescript-nestjs-npm-mysql/**"
      - "Database/mysql/shop/**"
      - "Tools/ci/load_mysql.sh"
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    working-directory: Backend/shop/shop-typescript-nestjs-npm-mysql

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: 1234
          MYSQL_DATABASE: mysql
          MYSQL_USER: admin
          MYSQL_PASSWORD: 1234
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -p1234"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=20
    env:
      MYSQL_HOST: 127.0.0.1
      MYSQL_PORT: 3306
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_USER: admin
      MYSQL_PASSWORD: 1234
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      DB_NAME: pb_shop
      DB_USER: admin
      DB_PASSWORD: 1234
      JWT_SECRET: ci-access-secret
      JWT_REFRESH_SECRET: ci-refresh-secret
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y default-mysql-client
        working-directory: .
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: Backend/shop/shop-typescript-nestjs-npm-mysql/package-lock.json
      - run: chmod +x Tools/ci/load_mysql.sh
        working-directory: .
      - run: bash Tools/ci/load_mysql.sh shop pb_shop
        working-directory: .
      - run: npm ci
      - run: npm run build
      - run: npm run test:e2e
