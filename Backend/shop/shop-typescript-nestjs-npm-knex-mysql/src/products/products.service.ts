import { Injectable } from "@nestjs/common"; @Injectable() export class ProductsService { private readonly items = [{ id: 1, categoryId: 1, name: 'Project Bible Handbook', description: 'Printed handbook', price: 19000, stock: 50, status: 'ACTIVE' }, { id: 2, categoryId: 2, name: 'Project Bible Mug', description: 'Ceramic mug', price: 12000, stock: 20, status: 'ACTIVE' }]; findAll(filterValue?: number) { return this.items.filter((item) => (filterValue ? item.categoryId === filterValue : true)).map(({ id, categoryId, name, price, stock, status }) => ({ id, categoryId, name, price, stock, status })); } findOne(id: number) { const item = this.items.find((candidate) => candidate.id === id); if (!item) throw new Error(`Not found: ${id}`); return item; } }
