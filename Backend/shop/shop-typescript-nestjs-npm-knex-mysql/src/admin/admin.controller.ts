import { Controller, Get } from "@nestjs/common"; import { ApiOperation, ApiTags } from "@nestjs/swagger"; import { ok } from "../common/api-response"; import { AdminService } from "./admin.service"; @ApiTags("admin") @Controller("api/v1/admin") export class AdminController { constructor(private readonly adminService: AdminService) {} @ApiOperation({ summary: "Current admin" }) @Get("me") me() { return ok(this.adminService.me()); } @ApiOperation({ summary: "Admin dashboard" }) @Get("dashboard") dashboard() { return ok(this.adminService.dashboard()); }}
