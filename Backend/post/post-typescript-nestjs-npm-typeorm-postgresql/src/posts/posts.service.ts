import { Injectable } from "@nestjs/common"; @Injectable() export class PostsService { private readonly items = [{ id: 1, boardId: 1, title: 'Welcome to Project Bible', content: 'Baseline post content', viewCount: 42, likeCount: 7, commentCount: 3, status: 'ACTIVE' }, { id: 2, boardId: 1, title: 'Second post', content: 'Another baseline post', viewCount: 15, likeCount: 2, commentCount: 1, status: 'ACTIVE' }]; findAll(filterValue?: number) { return this.items.filter((item) => (filterValue ? item.boardId === filterValue : true)).map(({ id, boardId, title, viewCount, likeCount, commentCount, status }) => ({ id, boardId, title, viewCount, likeCount, commentCount, status })); } findOne(id: number) { const item = this.items.find((candidate) => candidate.id === id); if (!item) throw new Error(`Not found: ${id}`); return item; } }
