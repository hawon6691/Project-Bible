import { Controller, Get } from "@nestjs/common"; import { ApiOperation, ApiTags } from "@nestjs/swagger"; import { listMeta, ok } from "../common/api-response"; import { BoardsService } from "./boards.service"; @ApiTags("boards") @Controller("api/v1/boards") export class BoardsController { constructor(private readonly service: BoardsService) {} @ApiOperation({ summary: "List boards" }) @Get() list() { const items = this.service.findAll(); return ok(items, listMeta(items.length)); } }
